langcode: en
status: true
dependencies:
  enforced:
    module:
      - farm_syntropic
id: infrastructure
label: Infrastructure
description: 'Farm infrastructure (solar, irrigation, fencing) with typed geometries.'
new_revision: true
