data_stream.data_stream:
  path: '/data_stream/{uuid}'
  defaults:
    _controller: '\Drupal\data_stream\Controller\DataStreamController::dataStream'
  requirements:
    # There is no access restriction to this endpoint.
    _access: 'TRUE'
