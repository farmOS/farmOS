langcode: en
status: true
dependencies:
  enforced:
    module:
      - farm_map
id: default
label: Default
description: 'The default farmOS map.'
behaviors:
  - asset_type_layers
options:
  asset_type_layers:
    all_locations:
      label: All locations
      filters:
        is_location: 1
      color: grey
      zoom: true
