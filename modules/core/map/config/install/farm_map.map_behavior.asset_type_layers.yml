langcode: en
status: true
dependencies:
  enforced:
    module:
      - farm_map
id: asset_type_layers
label: Asset type layers
description: 'Displays asset geometries in layers by asset type.'
library: 'farm_map/behavior_asset_type_layers'
settings: { }
